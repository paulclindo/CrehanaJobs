query FilterByCountry($countryId: ID!) {
  countries {
    jobs(where: { countries_every: { id: $countryId } }) {
      id
      title
      commitment {
        id
        title
      }
      company {
        id
        name
      }
      cities {
        id
        name
        country {
          id
          name
        }
      }
      company {
        id
        name
      }
      postedAt
    }
  }
}
